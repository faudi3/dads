const slider = document.getElementsByClassName("section");
document.addEventListener("scroll", (e) => {
  for (divs in slider) {
    let height = slider.style.height;
    let y = window.scrollY;
  }
});
